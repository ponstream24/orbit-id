{
  "name": "orbit-id",
  "private": true,
  "version": "1.0.1",
  "description": "Orbit ID monorepo",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @orbit-id/core && npm run build -w @orbit-id/typescript && npm run build -w @orbit-id/cli",
    "test": "npm run test -w @orbit-id/core && npm run test -w @orbit-id/typescript && npm run test -w @orbit-id/cli",
    "typecheck": "npm run typecheck -w @orbit-id/core && npm run typecheck -w @orbit-id/typescript && npm run typecheck -w @orbit-id/cli"
  },
  "engines": {
    "node": ">=20"
  }
}
